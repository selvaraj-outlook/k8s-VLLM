apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: gpu-inference-staging
resources:
  - ../../base
  - namespace.yaml
labels:
  - pairs:
      app.kubernetes.io/part-of: gpu-platform
      app.kubernetes.io/environment: staging
      app.kubernetes.io/managed-by: kustomize
# The image is set at deploy time; see the note in overlays/dev/kustomization.yaml.
